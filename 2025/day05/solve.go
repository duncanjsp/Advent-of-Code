package main

import (
	"strings"
)

func Solve(input string) (int, int) {
	lines := strings.Split(strings.TrimSpace(input), "\n")

	part1 := 0
	part2 := 0

	_ = lines // remove this once used
	return part1, part2
}
